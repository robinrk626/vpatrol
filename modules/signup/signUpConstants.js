const SUCCESS_MESSAGES = {
  SIGN_UP: 'Successfully Created Account'
};

const VALIDATION_MESSAGES = {
  EMAIL_ALREADY_USED: 'Email is already used',
};

module.exports = {
  SUCCESS_MESSAGES,
  VALIDATION_MESSAGES,
}